# חדר מצב כסף — הוראות ל-Claude Code

אתה עוזר לבעל עסק ישראלי, לאו דווקא טכני, להפעיל את הדשבורד הפיננסי הזה.
עברית פשוטה, בלי ז'רגון, בלי הסימן — (קו מפריד ארוך).

## מה יש כאן

דשבורד מקומי (Next.js + SQLite) שמציג יתרה, תזרים, מע"מ ומקדמות, מכירות
קארדקום וסיווג הוצאות, מנתוני בנקאות פתוחה (Financy). שני מסכים:
`/` (דוח מצב) ו-`/details` (פירוט). פירוט מלא ב-README.

## הפקודות שבעל העסק ישתמש בהן

| מה | איך |
|---|---|
| התקנה ראשונית / שינוי הגדרות | `/setup` (סקיל; שאלה אחת בכל פעם) |
| סקירת רואה חשבון פנימי | `/review` בשיחה, או `npm run review` |
| סיווג תנועות בלי קטגוריה | `/classify` בשיחה, או `npm run classify`; אישור בדשבורד |
| שאלות על הנתונים | הצ'אט הצף בדשבורד ("שאל את הנתונים") |
| נתוני הדגמה (עסק פיקטיבי) | `npm run demo` (מסרב לדרוס עסק אמיתי) |
| סנכרון ידני | `npm run sync` |
| טסטים | `npm test` (חייב לעבור לפני שמדווחים "מוכן") |

## חוקים שאסור לעקוף

1. **הסוכנים רצים רק על מנוי claude.ai של בעל העסק** (`claude auth login --claudeai`).
   לעולם אל תציע מפתח API, Bedrock, Vertex או ספק אחר, ואל תעקוף את הבדיקות
   ב-`scripts/lib/agent-run.mjs`. אם preflight מסרב (exit 2), הסבר את הסיבה
   שהודפסה ועצור.
2. **שום נתון לא נשלח ל-Claude לפני הסכמה.** ההסכמה נשמרת ב-
   `config/settings.json → agentsSendDataToClaude + agentsConsentVersion`,
   ומאושרת עם `npm run review -- --yes`. אל תדליק את הדגלים בעצמך; בקש
   מבעל העסק להריץ את הפקודה אחרי שהסברת מה נשלח (ההסבר מודפס שם).
3. **מפתחות וסודות** נשמרים רק דרך `saveSecret` (Keychain / ‎.env‎). לעולם אל
   תדפיס אותם, אל תכתוב אותם לקבצים אחרים, ואל תבקש אותם בצ'אט אם אפשר במסך.
4. **סיווג תנועות** משנים רק דרך אישור בדשבורד או `applyProposal` /
   `removeRule` מ-`scripts/lib/classify-agent.mjs`, לא ב-UPDATE ידני על המסד.
   החלטות בעל העסק יושבות ב-SQLite (`classify_rules`) וגוברות על הכל.
5. **`config/` הוא הכיול של העסק.** אל תערוך בלי להסביר קודם מה ישתנה ולקבל
   "כן". אל תריץ `npm run demo -- --force` על עסק אמיתי בלי אישור מפורש
   (הוא מגבה ל-`data/config-backup-*` ואז מאפס הכל).
6. **הנתונים ב-`data/` הם כספי העסק.** אין מחיקות שם בלי אישור מפורש.
7. אחרי כל שינוי קוד: `npm test` (ואם נגעת ב-UI, גם `npm run build`).

## איפה מה נמצא

```
lib/queries.js              כל צד הקריאה (P&L, תזרים, מע"מ, איכות נתונים)
scripts/lib/classify.mjs    מנוע הסיווג; חוקים מפורשים מ-classify_rules קודמים
scripts/lib/agent-run.mjs   שער הסוכנים: הסכמה, מנוי, סביבה, הרצה מוגבלת
scripts/lib/ask.mjs         הצ'אט: חבילת נתונים + SELECT על מסד-צל מוגבל
config/                     הכיול; config.defaults/ = ברירות המחדל שנשלחות
data/                       SQLite + פלט סוכנים (לא ב-git)
.claude/skills/             setup / review / classify
```
